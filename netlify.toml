[build]
  command = "npm run maj:ffvb && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Les en-tetes de securite sont dans public/_headers, pour qu'ils
# s'appliquent aussi a un dossier depose sur Netlify Drop.
